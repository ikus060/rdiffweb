# Copyright (C) 2023 IKUS Software. All rights reserved.
# IKUS Software inc. PROPRIETARY/CONFIDENTIAL.
# Use is subject to license terms.

[tox]
envlist = py3, flake8, black, isort

[testenv]
deps =
  pytest-cov
setenv =
  PIP_EXTRA_INDEX_URL = https://nexus.ikus-soft.com/repository/pypi-group/simple/
commands_pre=
  pip install -e .[test]
commands =
  pytest -v --debug --override-ini junit_family=xunit1 --junit-xml=xunit.xml --cov=minarca_server --cov-report xml:coverage.xml minarca_server

[testenv:black]
deps = black==23.1.0
commands = black --check --diff minarca_server
skip_install = true

[testenv:flake8]
deps =
  flake8
commands = flake8 minarca_server
skip_install = true

[testenv:isort]
deps = isort>=5.0.1
commands = isort --check --diff minarca_server
skip_install = true

[testenv:pyinstaller]
deps =
  pyinstaller<6
  debbuild==0.1.0
commands =
  pyinstaller packaging/minarca-server.spec --noconfirm

[flake8]
ignore =
  # whitespace before ':'
  E203
  # line too long (86 > 79 characters)
  E501
  # line break before binary operator
  W503
  # ambiguous variable name 'I'
  E741
filename =
  *.py
max-complexity = 20

[isort]
profile = black
line_length = 120